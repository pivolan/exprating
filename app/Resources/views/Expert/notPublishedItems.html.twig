{% extends 'Profile/user.html.twig' %}
{% block title %}Мои не опубликованные обзоры{% endblock %}
{% block breadcrumbs %}
    {{ parent() }}
    → <a href="{{ path("expert_not_published_items") }}">Мои не опубликованные обзоры</a>
    {% if category %}
        → <a href="{{ path("expert_not_published_items", {"slug": category.slug}) }}">{{ category.name }}</a>
    {% endif %}
{% endblock %}
{% block profile_body %}
    <h1>Мои не опубликованные обзоры</h1>

    <div class="col-md-4">
        <div id="treeCategory"></div>
    </div>
    <div class="col-md-8 history_products">
        {{ include("Expert/part.html.twig") }}
    </div>
{% endblock %}
{% block javascripts %}
    {{ parent() }}
    <script type="text/javascript">$(function () {
            $('[data-toggle="tooltip"]').tooltip()
        })
    </script>
    {{ include("_include/Category/_jsTree.html.twig", {'categories': user.categories, 'category': category, 'treeId': 'treeCategory'}) }}
{% endblock %}